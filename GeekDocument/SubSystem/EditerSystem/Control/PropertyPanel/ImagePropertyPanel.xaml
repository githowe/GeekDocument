<local:PropertyPanel x:Class="GeekDocument.SubSystem.EditerSystem.Control.PropertyPanel.ImagePropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GeekDocument.SubSystem.EditerSystem.Control.PropertyPanel"
             xmlns:pb="clr-namespace:GeekDocument.SubSystem.EditerSystem.Control.PropertyBar"
             mc:Ignorable="d" d:FontFamily="NSimSun"
             d:DesignWidth="320" d:DesignHeight="600">
    <Grid x:Name="MainGrid">
        <Grid.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Duration="0:0:0.5" To="1" Storyboard.TargetName="MainScrollBar" Storyboard.TargetProperty="Opacity"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Duration="0:0:0.5" To="0" Storyboard.TargetName="MainScrollBar" Storyboard.TargetProperty="Opacity" BeginTime="0:0:2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
        <StackPanel x:Name="MainStackPanel" VerticalAlignment="Top">
            <Expander Header="图片" IsExpanded="True">
                <StackPanel Orientation="Vertical" Margin="10">
                    <pb:TextInput x:Name="Bar_SourceWidth" Title="源宽度" ReadOnly="True"/>
                    <pb:TextInput x:Name="Bar_SourceHeight" Title="源高度" ReadOnly="True" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_Width" Title="目标渲染宽度" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_ActualWidth" Title="实际渲染宽度" ReadOnly="True" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_ActualHeight" Title="实际渲染高度" ReadOnly="True" Margin="0,10,0,0"/>
                    <pb:OnOff x:Name="Bar_PixelArt" Title="像素画" Margin="0,10,0,0"/>
                </StackPanel>
            </Expander>
            <Expander Header="图注" IsExpanded="True">
                <StackPanel Orientation="Vertical" Margin="10">
                    <pb:Selecter x:Name="Bar_Font" Title="字体"/>
                    <pb:TextInput x:Name="Bar_Size" Title="字号" Margin="0,10,0,0"/>
                </StackPanel>
            </Expander>
            <Expander Header="动画" IsExpanded="True">
                <StackPanel Orientation="Vertical" Margin="10">
                    <pb:TextInput x:Name="Bar_FrameCount" Title="帧数" ReadOnly="True"/>
                    <pb:TextInput x:Name="Bar_Fps" Title="帧率" ReadOnly="True" Margin="0,10,0,0"/>
                    <pb:ToolBar x:Name="Bar_PlayBar" Title="播放控制" Margin="0,10,0,0"/>
                </StackPanel>
            </Expander>
            <Expander Header="布局" IsExpanded="True">
                <StackPanel Orientation="Vertical" Margin="10">
                    <pb:RadioButtonBar x:Name="Bar_Align" Title="对齐方式"/>
                    <pb:TextInput x:Name="Bar_MarginTop" Title="上边距" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_MarginBottom" Title="下边距" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_MarginLeft" Title="左边距" Margin="0,10,0,0"/>
                    <pb:TextInput x:Name="Bar_MarginRight" Title="右边距" Margin="0,10,0,0"/>
                </StackPanel>
            </Expander>
        </StackPanel>
        <ScrollBar x:Name="MainScrollBar" Orientation="Vertical" HorizontalAlignment="Right" Tag="0" Width="2" Opacity="0"
                   SmallChange="1" ViewportSize="0" Minimum="0" Maximum="0"/>
    </Grid>
</local:PropertyPanel>