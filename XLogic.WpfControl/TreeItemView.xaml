<UserControl x:Class="XLogic.WpfControl.TreeItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XLogic.WpfControl"
             mc:Ignorable="d" Visibility="Hidden"
             d:DesignWidth="300" Height="23">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/ButtonStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="Back" Background="Transparent" MouseLeftButtonDown="Back_MouseLeftButtonDown">
        <!-- 内容 -->
        <Grid x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="23"/>
                <ColumnDefinition Width="23"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <!-- 箭头 -->
            <ToggleButton x:Name="Arrow" Style="{StaticResource TreeItemArrow}" Click="Arrow_Click"/>
            <!-- 图标 -->
            <Image x:Name="Image_Icon" Width="15" Height="15" Grid.Column="1"/>
            <!-- 文本 -->
            <TextBlock x:Name="TB_Text" Text="文本" Grid.Column="2" Foreground="White" Margin="6,0,5,0" FontSize="12" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>