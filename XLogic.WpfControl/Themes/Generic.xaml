<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:XLogic.WpfControl">

    <Style x:Key="Numeric" TargetType="RepeatButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border x:Name="MainBorder" Background="Transparent">
                        <ContentPresenter x:Name="content" UseLayoutRounding="True" Opacity="0.25"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="content" Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="content" Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:NumericUpDown}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                    <Grid x:Name="MainGrid">
                        <TextBox x:Name="PART_TextBox"/>
                        <RepeatButton x:Name="PART_Up" Width="17" Height="11" Style="{StaticResource Numeric}" Focusable="False"
                              HorizontalAlignment="Right" VerticalAlignment="Top" Margin="1">
                            <Image x:Name="Image_Up" Source="XLogic.WPFControl;component/Image/Arrow.png" Width="9" Height="7"/>
                        </RepeatButton>
                        <RepeatButton x:Name="PART_Down" Width="17" Height="11" Style="{StaticResource Numeric}" Focusable="False"
                              HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="1">
                            <Image x:Name="Image_Down" Source="XLogic.WPFControl;component/Image/Arrow.png" Width="9" Height="7">
                                <Image.LayoutTransform>
                                    <RotateTransform Angle="180"/>
                                </Image.LayoutTransform>
                            </Image>
                        </RepeatButton>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_TextBox" Property="Opacity" Value="0.2"/>
                            <Setter TargetName="Image_Up" Property="Opacity" Value="0.2"/>
                            <Setter TargetName="Image_Down" Property="Opacity" Value="0.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>